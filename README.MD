`.bash_profile` can be included with
```sh
config_dir=~/Development/configs
source "$config_dir/.bash_profile"
```

`.gitconfig` can be included with
```ini
[include]
    path = /path/to/.gitconfig
```
